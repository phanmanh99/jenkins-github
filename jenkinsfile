pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/phanmanh99/jenkins-github.git'
                sh 'ls'
                sh "docker ps"
              }
          }
      }
  }
